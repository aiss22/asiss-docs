# 获取分录行数量

`this.View.Model.GetEntryRowCount("分录标识");` 获取分录行数

```示例
//获取分录行数量
var Rows = this.View.Model.GetEntryRowCount("FSaleOrderEntry").ToString();
//把获取的数量赋值给备注FNote
this.View.Model.SetValue("FNote", Rows);
```
